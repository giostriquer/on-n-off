# Every Swift helper builds into its own `.build` directory during a cargo build; watching
# them would restart `tauri dev` on each build, including the manual `swift build` runs and
# the native checks. Covers macos/SideNotch and macos/BrowserBilling.
macos/*/.build/
